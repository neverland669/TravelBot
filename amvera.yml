meta:
  environment: python
  toolchain:
    name: pip
    version: "3.8"
build:
  requirementsPath: requirements.txt
run:
  command: gunicorn --bind 0.0.0.0:80 app:main
  persistenceMount: /data
  containerPort: "80"
